for (let index = 1; index <= 10; index++) {
    for (let index2 = 1; index2 <= 10; index2++) {
      let product = index * index2;
      console.log(index + " * " + index2 + " = " + product);
    }
  }